# testthat bootstrap file
# Run all tests with: testthat::test_dir("tests/testthat")

library(testthat)

# Source the utility functions (but skip the data loading parts for unit tests)
# We'll source individual functions as needed in each test file
